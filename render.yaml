services:
  - type: web
    name: shopping-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: DB_PATH
        value: /tmp/shopping.db
